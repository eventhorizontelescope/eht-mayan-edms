apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: bridge
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/bridge: "true"
  podSelector:
    matchLabels:
      io.kompose.network/bridge: "true"
